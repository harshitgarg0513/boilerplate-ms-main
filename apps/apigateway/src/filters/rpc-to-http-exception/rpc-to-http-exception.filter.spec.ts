import { GatewayRpcToHttpExceptionFilter } from '@harshitgarg0513/error-handling';

describe('GatewayRpcToHttpExceptionFilter', () => {
  it('should be defined', () => {
    expect(new GatewayRpcToHttpExceptionFilter()).toBeDefined();
  });
});
